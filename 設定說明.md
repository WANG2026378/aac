# 流量表 ＋ 悄悄話留言板 · 設定說明

一次性設定約 10 分鐘。設定完成前，網站一切照常運作，只是流量不會被記錄。

---

## 第一步：建立後端（Google 試算表 ＋ Apps Script）

1. 開一個新的 Google 試算表，命名為「**永芳特教網站後台**」。
2. 上方選單 **擴充功能 → Apps Script**。
3. 把編輯器裡原有的 `function myFunction() {}` 全部刪掉，貼上 **`Code.gs`** 的完整內容，按存檔。

## 第二步：設定管理員密碼

在剛貼上的 `Code.gs` **最上面**找到這一段，把 `ADMIN_KEY` 那行的內容換成你自己想的密碼，存檔：

```js
var CONFIG = {
  ADMIN_KEY: '換成你的管理員密碼',      // ← 只改這行
  SALT: 'F_kqIVHsEBvTBPFTVLFveugoSnrIaarm'   // ← 不要動
};
```

- `SALT` 已經幫你產好了，**不要改也不要洩漏**（改了所有留言者的密碼會失效）。
- 密碼寫在這裡不會外洩：這支檔案只存在你的 Apps Script 專案，網站訪客拿不到。
- 沒改就部署也不會出事——管理員功能會全部進不去，不會變成任何人都能看留言。

試算表的四個分頁會在第一次有人使用時自動建立，**不需要手動執行 `setup()`**。

## 第三步：部署

1. 右上角 **部署 → 新增部署作業**。
2. 齒輪選 **網頁應用程式**。
3. **執行身分**：我（你的帳號）
4. **具有應用程式存取權的使用者**：**所有人**
   （必須選這個，學生和家長才呼叫得到。實際能看到什麼由密碼決定。）
5. 第一次會跳授權：選你的 Google 帳號 →「**進階**」→「**前往 …（不安全）**」→ 允許。這是你自己的專案，正常流程。
6. 按部署，**複製那串網址**，長得像
   `https://script.google.com/macros/s/AKfycb.....xyz/exec`

## 第四步：把網址填進網站

打開 **`offline-register.js`**，找到 `window.AAC_API_URL` 那一行：

```js
window.AAC_API_URL = '';
```

把剛剛複製的網址貼進單引號中間。**只有這一個地方要改。**

## 第五步：上線

把這五個檔案放進 `~/Desktop/aac_inbox`，雙擊 `急件立刻上線.command`：

- `msg.html`（新）
- `stats.html`（新）
- `offline-register.js`（已改：加了流量記錄）
- `index.html`（已改：加了留言板入口、工具卡片點閱數）
- `sw.js`（已改：版本號 bump 成 `20260806-msg-stats-hits-v2`）

---

## 之後怎麼用

- **留言板**：`wang2026378.github.io/aac/msg.html`，首頁上下都有入口。
- **流量表**：`wang2026378.github.io/aac/stats.html`，首頁**沒有連結**，自己加書籤即可。也可以從留言板的「管理員」分頁按鈕進去。
- 兩個頁面的管理員密碼都是 `ADMIN_KEY`。

## 卡片點閱數

首頁每張工具卡片右下角會顯示 **👀 累計點閱數**，資料就是流量記錄裡該頁的次數，不必另外設定。

- **從 0 開始算。** 上線那天所有卡片都沒有數字，之後才慢慢長出來。
- **0 次的卡片不顯示徽章**，不會整排掛著難看的 0。
- 數字後端快取 5 分鐘，所以剛點完不會立刻加一。
- 這個數字是**公開的**，任何看首頁的人都看得到。如果你不想讓冷門工具的低數字被家長看到，跟我說一聲，可以改成只有管理員看得到、或是滿 N 次才顯示。
- 口徑跟流量表一致：同一頁 30 分鐘內只算一次。

## 需要知道的幾件事

**留言的隱私是後端擋的，不是前端。** 學生查詢時，伺服器只把身分碼吻合的留言送回瀏覽器，別人的留言根本不會離開試算表。管理員權限同理。你也可以直接打開試算表的「留言」分頁看，效果一樣。

**密碼沒有存明碼**，試算表裡的「身分碼」是 `SALT＋暱稱＋密碼` 的 SHA-256。好處是外流也看不出密碼，代價是**學生忘記密碼你無法幫他查**——只能請他換一個新暱稱重新開始。

**暱稱先到先得。** 有人用了「小恐龍」，別人再用同一個暱稱但密碼不對就會被擋，不會誤看到別人的留言。

**流量記錄的口徑**：同一頁 30 分鐘內只算一次；訪客代號是隨機字串存在該裝置，不含任何個人資料；共用 iPad 上多個學生會被算成同一位訪客，這點無解，看趨勢就好。

**改了 `Code.gs` 之後要重新部署**：部署 → **管理部署作業** → 鉛筆 → 版本選「**新版本**」→ 部署。網址不變。如果只按「新增部署作業」會拿到一個新網址，舊網址仍跑舊程式。

**試算表會長大。** Apps Script 裡有個 `壓縮舊流量()` 函式，會把 90 天前的原始資料壓成每日彙總（壓縮前會先把各頁次數存進「頁面累計」分頁，所以卡片上的點閱數不會倒退）。流量大的話設一個每月觸發程序跑它；一般用量放著不管也沒關係。

**免費額度**：Apps Script 一般帳號每天 20,000 次請求，校內使用規模遠遠用不完。

---

## 測試順序建議

1. 先開 `msg.html`，用假暱稱留一則試試看，確認試算表「留言」分頁有進資料。
2. 切「管理員」分頁輸入 `ADMIN_KEY`，回覆那則留言。
3. 切回「我的留言」，用同一組暱稱密碼確認看得到回覆。
3.5. 回首頁重整幾次，等 5 分鐘快取過期後，首頁卡片應該開始出現點閱數。
4. 開 `stats.html` 輸入 `ADMIN_KEY`，確認今天的數字有跳動。

測試時記得 **Cmd+Shift+R 強制重新整理**，舊快取會擋。
